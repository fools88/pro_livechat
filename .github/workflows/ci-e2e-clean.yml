name: CI - Server tests + E2E (cleaned)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: No-op
        run: |
          echo "ci-e2e-clean placeholder: workflow validated at $(date)"
