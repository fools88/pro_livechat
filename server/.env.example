# Example env file for local development / CI-like runs
# DO NOT commit real secrets to source control. Use this only as a template.

# Database
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=prochat_db

# App
PORT=8081
LOG_LEVEL=debug

# Feature flags
# Set to false in production to disable legacy widgetKey fallback
ALLOW_LEGACY_WIDGET_KEY=false

# Mock flags for local development (set to true to avoid external AI/Vector dependencies)
MOCK_AI=true
MOCK_VECTOR=true

# JWT secret (use a secure random value in CI/production)
JWT_SECRET=ci-test-secret
# Example .env for Pro Livechat (do NOT commit secrets)

# Database
DB_NAME=prochat_db
DB_USER=prochatadmin
DB_PASSWORD=REPLACE_ME_DB_PASSWORD
DB_HOST=localhost
DB_PORT=5432
DB_DIALECT=postgres

# App
PORT=8080
JWT_SECRET=your_jwt_secret_here

# Google Gemini (Generative AI)
GOOGLE_GEMINI_API_KEY=MASUKKAN_KEY_GEMINI_KAMU_DI_SINI

# Pinecone (Vector DB)
PINECONE_API_KEY=MASUKKAN_KEY_PINECONE_KAMU_DI_SINI
PINECONE_ENVIRONMENT=your-pinecone-environment
PINECONE_INDEX_NAME=prochat-index

# S3 / MinIO
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
AWS_ACCESS_KEY_ID=REPLACE_ME_MINIO_USER
AWS_SECRET_ACCESS_KEY=REPLACE_ME_MINIO_PASSWORD
S3_BUCKET_NAME=prochat-uploads

# Optional: other envs
# VITE_API_URL=http://localhost:8080

# ===== Mocking for local tests/CI =====
# Set these to 'true' to run the server with lightweight in-process mocks
# for Google Gemini and Pinecone. Useful for CI or local runs where you don't
# want to rely on external paid services.
# Example (Windows cmd.exe):
#   set MOCK_AI=true&& set MOCK_VECTOR=true&& npm run start
MOCK_AI=false
MOCK_VECTOR=false


